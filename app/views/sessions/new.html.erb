<div class="container mt-5 my-5">
  <div class="offset-3 col-6 b-color p-0">
    <h5 class="text-center m-color py-3 text-white font-weight-bold">Log in</h5>
    <%= form_with(url: login_path, scope: :session, local: true) do |f| %>

    <div class="px-4 mt-4">
      <div class="form-group">
        <%= f.label :username %>
        <%= f.text_field :username, class: 'form-control', required: true %>
      </div>

      <div class="form-group d-flex justify-content-between">
        <span class='t-color mt-1'>Login with: 
          <%= link_to image_tag('twitter.png', class: 'l-s img-fluid'), "/auth/twitter"%>
          <%= link_to image_tag('google.png', class: 'l-s img-fluid'), "/auth/google_oauth2"%>
        </span>
        <span>
          <%= link_to "Register", new_user_path, class: 'pr-2 t-color' %>
          <%= f.submit 'Log in', class: 'buttons' %>
        </span>
      </div>
    </div> 
    <% end %>
  </div>
</div>
